# The Office V8.4

- `OFFICE_OK` is now a first-class runtime canary.
- The canary still goes through the external provider but is wrapped in an exact-output contract.
- Reviewer is skipped for this deterministic health check.
- Normal prompts retain the standard Director + Skeptic flow.
